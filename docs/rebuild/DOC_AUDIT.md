# Markdown 与源码核对

仓库 Markdown 是历史资料，不直接作为已验证能力。以下结论记录改造前 `38f37ce` 的源码核对结果；当前实现以最新 `main`、`README.md` 和 `docs/rebuild/STATUS.md` 为准。本轮源码与配置修复提交为 `a28ebe0`、`1310859`、`5a5b943`、`22a955d`、`17a0d46`、`1866894`、`c175966`、`c202737`、`2486587`、`41571a0`、`cbef1eb`、`d37bab2`、`4c84673`、`bd1baf9`、`43d5a3c`、`4ef43ea`、`cc0538a`、`b3a0fda`、`79713bf`、`de15ec6`、`a68fb67`、`efa2fc4`、`c23c391`；当前代码对照提交为 `c23c391`。

本轮修复已将旧的 Coze、启动和 Railway 说明改为当前版本或明确标注为历史资料；任何未核验的平台文档都不能作为真实接入证据。上述提交包含源码、配置、CI 与文档同步；GitHub Actions 运行 `35431605469`（提交 `c23c391`）已完成并通过，较早的 `35430738739` 对应 `efa2fc4`、`35426614604` 对应 `a68fb67`、`35404297806` 对应 `79713bf`；真实平台权限与业务并发验收仍不属于源码通过证据。

`007_ai_provider_recovery.sql` 之后的 `008_ai_run_legacy_recovery.sql` 将迁移边界前的全部 AI run 标记为历史依据未知，禁止把当前知识冒充旧请求依据或自动重发；新任务使用 `pending → captured` 快照状态。Worker 的失败状态同步已在 `cbef1eb` 原子化，`d37bab2`、`4c84673` 和 `bd1baf9` 又补齐了取消消息的受控人工恢复、前端会话与视图代际保护、Redis 订阅旧连接回收、Coze 会话串行／4016 可重试分类、失败租约 fencing、访客可见性序列分页，以及独立 advisory-lock 连接池；`43d5a3c` 进一步隔离了 ChatWidget 路由切换时旧轮询对新会话的影响；`4ef43ea`、`cc0538a` 修复了旧游标兼容、访客排序、客服模式切换回调，并新增 `010_ai_run_legacy_recovery_correction.sql` 纠正已记录旧版 008 的存量边界；`b3a0fda` 又为模式写入增加版本 fencing 与冲突重试，并将新旧消息混合排序改为传递的统一全序；`79713bf` 将 `expectedModeVersion` 收紧为必填的有界 number，并为缺失、布尔、字符串、浮点和越界输入增加回归测试。上述代码状态仍需结合真实 PostgreSQL/Redis、浏览器和供应商验收，不把隔离测试当作真实平台证据。
`de15ec6` 修复了 CRA 开发热更新与应用 WebSocket 路径冲突，并为登录失败增加稳定的页面内错误提示；`a68fb67` 同步了 README 的开发 WebSocket 说明。上述代码状态仍需结合真实 PostgreSQL/Redis、浏览器和供应商验收，不把隔离测试当作真实平台证据。
`efa2fc4` 恢复了真实配置驱动的抖音网站应用 OAuth：管理员生成一次性扫码请求，回调绑定原管理员会话并防回放，token 与 refresh token 仅加密落库，前端通过状态轮询得到结果；本地已验证生成 URL、拒绝授权和重复回调边界，但没有把这当作真实抖音平台通过证据。评论、私信、直播和回复能力继续保持 `unverified`。
`c23c391` 根据后续审计收紧 OAuth 闭环：最终落库按认证模块的用户→会话锁序重读撤销状态；通用 PATCH 不能将抖音状态伪造为 `authorized/subscribed`，授权展示同时要求加密凭据存在；失败或过期的一次性二维码不再显示为可用；新迁移 `012` 以复合外键绑定 OAuth request 与同租户、同用户的 auth session。

## 关键冲突

| 文档结论 | 源码事实 | 处理 |
|---|---|---|
| OAuth 后可以通过 live-im-proxy 监听 | 历史 Go OAuth、账号保存和渠道监听为内存实现；当前 Node 已重做账号 OAuth，但消息协议仍未真实验证 | 账号授权与消息能力分开标记，消息侧继续 `unverified`，T7 单独核验 |
| 已支持多平台/私信/评论回复 | Go pipeline 只放行 `comment`，不同平台实现和权限没有真实证据 | 不标记为已接入 |
| 测试完成/生产可用 | 现有测试主要是参数和模拟路径，业务记录没有数据库持久化 | 重新建立可重复验收 |
| 两仓库三服务是当前部署结构 | 当前代码是单仓库，Node、Go、前端、Compose 配置职责重叠 | T1 统一 Compose，Go 设为可选适配器 |
| MySQL/PostgreSQL 均可用 | Node 没有数据库访问层，Compose 仅声明 PostgreSQL | 统一 PostgreSQL |

## 重点文件结论

- `README.md`、`RAILWAY_DEPLOY.md`、`启动说明.md`：保留为部署参考，README 的访客分页口径已同步为可见性序列及旧游标兼容；T9 更新为当前单仓库结构。
- `实现进度报告.md`、`测试结果总结.md`、`模拟测试报告.md`：不能作为真实收发证据。
- `技术方案重新梳理.md`、`抖音接口使用说明.md`、`access_token与live-im-proxy对接分析.md`：涉及平台协议的部分需要官方文档和真实账号重新核验。
- `Coze配置指南.md`：当前指南已按 Node 的 Coze v3 调用链对齐；其余历史文档中的旧式调用仍不能作为真实接入证据。
- 历史文档中出现疑似应用密钥的位置已纳入 T1 脱敏清单；删除当前树中的值不等于凭据已失效，必须由账号所有者轮换。

## 最新审计跟进

针对审计提交 `a662962` 发现的 E01–E04，`4ef43ea`／`cc0538a` 已完成以下闭环：纯时间旧游标保留原始排他时间边界；访客接口返回可见性序列且前端使用无损十进制排序；客服模式切换按会话与请求代次丢弃迟到结果；新增迁移 `010` 仅将旧版 008 边界前仍为 `pending` 的 AI run 标为 `unknown`，不覆盖已经捕获的快照。针对后续 S01/S02，`b3a0fda` 又让模式修改使用 `expectedModeVersion` 条件写入并对当前意图冲突重读重试，同时让缺失序列的旧消息与新序列消息使用统一传递排序规则，并加入回归测试；`79713bf` 进一步禁止模式接口省略或伪造版本参数，使每次模式写入都经过条件更新。`de15ec6` 修复开发 WebSocket 与 CRA HMR 路径冲突并增强登录失败反馈，`a68fb67` 同步 README；`efa2fc4` 补齐抖音账号 OAuth 的安全持久化链路，`c23c391` 关闭会话撤销竞态、状态伪造、失效二维码和会话归属约束四项后续问题。README、迁移状态和 CI 记录已同步。最终是否可标记“代码问题清零”，仍以原审计聊天对当前最新 `main` 的源码复核为准。
