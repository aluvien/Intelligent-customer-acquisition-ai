# Markdown 与源码核对

仓库 Markdown 是历史资料，不直接作为已验证能力。以下结论记录改造前 `38f37ce` 的源码核对结果；当前实现以最新 `main`、`README.md` 和 `docs/rebuild/STATUS.md` 为准。本轮源码与配置修复提交为 `a28ebe0`、`1310859`、`5a5b943`、`22a955d`、`17a0d46`、`1866894`、`c175966`、`c202737`、`2486587`、`41571a0`、`cbef1eb`、`d37bab2`、`4c84673`、`bd1baf9`、`43d5a3c`、`4ef43ea`、`cc0538a`、`b3a0fda`；当前对照提交为 `b3a0fda`。

本轮修复已将旧的 Coze、启动和 Railway 说明改为当前版本或明确标注为历史资料；任何未核验的平台文档都不能作为真实接入证据。上述提交包含源码、配置、CI 与文档同步；GitHub Actions 运行 `35402463706`（提交 `b3a0fda`）已通过，较早的 `35400341470` 对应 `cc0538a`；真实平台权限与业务并发验收仍不属于源码通过证据。

`007_ai_provider_recovery.sql` 之后的 `008_ai_run_legacy_recovery.sql` 将迁移边界前的全部 AI run 标记为历史依据未知，禁止把当前知识冒充旧请求依据或自动重发；新任务使用 `pending → captured` 快照状态。Worker 的失败状态同步已在 `cbef1eb` 原子化，`d37bab2`、`4c84673` 和 `bd1baf9` 又补齐了取消消息的受控人工恢复、前端会话与视图代际保护、Redis 订阅旧连接回收、Coze 会话串行／4016 可重试分类、失败租约 fencing、访客可见性序列分页，以及独立 advisory-lock 连接池；`43d5a3c` 进一步隔离了 ChatWidget 路由切换时旧轮询对新会话的影响；`4ef43ea`、`cc0538a` 修复了旧游标兼容、访客排序、客服模式切换回调，并新增 `010_ai_run_legacy_recovery_correction.sql` 纠正已记录旧版 008 的存量边界；`b3a0fda` 又为模式写入增加版本 fencing 与冲突重试，并将新旧消息混合排序改为传递的统一全序。上述代码状态仍需结合真实 PostgreSQL/Redis、浏览器和供应商验收，不把隔离测试当作真实平台证据。

## 关键冲突

| 文档结论 | 源码事实 | 处理 |
|---|---|---|
| OAuth 后可以通过 live-im-proxy 监听 | Go 的 OAuth、账号保存和渠道监听仍为内存实现，WS 协议未被真实验证 | 标记 `unverified`，T7 单独核验 |
| 已支持多平台/私信/评论回复 | Go pipeline 只放行 `comment`，不同平台实现和权限没有真实证据 | 不标记为已接入 |
| 测试完成/生产可用 | 现有测试主要是参数和模拟路径，业务记录没有数据库持久化 | 重新建立可重复验收 |
| 两仓库三服务是当前部署结构 | 当前代码是单仓库，Node、Go、前端、Compose 配置职责重叠 | T1 统一 Compose，Go 设为可选适配器 |
| MySQL/PostgreSQL 均可用 | Node 没有数据库访问层，Compose 仅声明 PostgreSQL | 统一 PostgreSQL |

## 重点文件结论

- `README.md`、`RAILWAY_DEPLOY.md`、`启动说明.md`：保留为部署参考，README 的访客分页口径已同步为可见性序列及旧游标兼容；T9 更新为当前单仓库结构。
- `实现进度报告.md`、`测试结果总结.md`、`模拟测试报告.md`：不能作为真实收发证据。
- `技术方案重新梳理.md`、`抖音接口使用说明.md`、`access_token与live-im-proxy对接分析.md`：涉及平台协议的部分需要官方文档和真实账号重新核验。
- `Coze配置指南.md`：当前指南已按 Node 的 Coze v3 调用链对齐；其余历史文档中的旧式调用仍不能作为真实接入证据。
- 历史文档中出现疑似应用密钥的位置已纳入 T1 脱敏清单；删除当前树中的值不等于凭据已失效，必须由账号所有者轮换。

## 最新审计跟进

针对审计提交 `a662962` 发现的 E01–E04，`4ef43ea`／`cc0538a` 已完成以下闭环：纯时间旧游标保留原始排他时间边界；访客接口返回可见性序列且前端使用无损十进制排序；客服模式切换按会话与请求代次丢弃迟到结果；新增迁移 `010` 仅将旧版 008 边界前仍为 `pending` 的 AI run 标为 `unknown`，不覆盖已经捕获的快照。针对后续 S01/S02，`b3a0fda` 又让模式修改使用 `expectedModeVersion` 条件写入并对当前意图冲突重读重试，同时让缺失序列的旧消息与新序列消息使用统一传递排序规则，并加入回归测试。README、迁移状态和 CI 记录已同步。最终是否可标记“代码问题清零”，仍以原审计聊天对 `b3a0fda` 的最新源码复核为准。
