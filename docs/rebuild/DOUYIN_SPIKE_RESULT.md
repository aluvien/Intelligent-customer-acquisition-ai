# 抖音最小联调结果

状态：blocked

未执行真实平台请求。缺少当前应用的有效权限、授权账号、测试内容和平台端收发证据；因此没有生成或保存任何真实 token、消息 ID 或发送回执，也没有把 Go 旧客户端接入 Node 业务链路。

解除阻塞条件：

1. 提供应用当前有效的官方权限审批记录和回调配置。
2. 使用专用测试账号完成一次授权，并以脱敏方式保存授权结果。
3. 选择一个官方明确允许的只读能力，先读取一条真实消息。
4. 只有在用户明确授权一次测试回复后，发送一条测试内容并在平台端核对回执。
5. 将测试证据按租户/账号命名空间保存，随后才能进入 T8。
