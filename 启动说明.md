# 星链云客系统启动说明（当前版本）

当前可验证的业务链路是 Node API + React 管理后台 + PostgreSQL + Redis + Web 客户入口。抖音等平台适配器仍保持 `PLATFORM_UNVERIFIED`，不会伪造授权或收发成功。

## Docker Compose

```bash
cp .env.template .env
# 在 .env 中填写随机 JWT_SECRET、32 字节 base64 ENCRYPTION_KEY、数据库密码和 CORS_ORIGIN
docker compose up -d --build
curl -fsS http://localhost:3000/readyz
```

初始化管理员时只通过临时环境变量传密码：

```bash
docker compose exec -e ADMIN_USERNAME=your-admin \
  -e ADMIN_EMAIL=you@example.com \
  -e ADMIN_PASSWORD='replace-with-a-long-password' \
  -e TENANT_NAME='你的企业' backend npm run init-admin
```

前端地址默认是 `http://localhost:3000/login`，API 由 Nginx 代理到 `/api`，就绪检查是 `/readyz`。仓库没有默认管理员密码，也不应把真实密钥写入脚本、日志或 Markdown。

## 本地分别启动

准备本机 PostgreSQL 和 Redis 后：

```bash
cp .env.template .env
cd backend && npm ci && npm run build && npm run migrate && npm run dev
cd ../frontend && npm ci && npm start
```

若在 Docker 中使用开发组合：

```bash
docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build
```

更完整的配置、能力边界和验证命令见 [`README.md`](README.md) 与 [`docs/rebuild/STATUS.md`](docs/rebuild/STATUS.md)。
