# 部署快速指南（当前单仓库结构）

本文替代历史的“两仓库三服务/Railway 分拆”说明。当前部署入口是根目录 `docker-compose.yml`：前端 Nginx、Node API/Worker、PostgreSQL 和 Redis；Go 程序只作为未核验平台适配器 profile 保留。

## 启动

```bash
cp .env.template .env
# 填写 DB_NAME、DB_USER、DB_PASSWORD、JWT_SECRET、ENCRYPTION_KEY、CORS_ORIGIN
docker compose up -d --build
docker compose ps
curl -fsS https://你的域名/readyz
```

生产环境的 `CORS_ORIGIN` 必须是管理后台的明确 HTTPS 来源，`COOKIE_SECURE=true`，不要使用通配符。Coze 只配置在 Node 服务端：

```dotenv
AI_PROVIDER=coze
COZE_API_URL=https://api.coze.cn/v3
COZE_BOT_ID=your-bot-id
COZE_TOKEN=your-server-token
```

首次初始化管理员见 [`启动说明.md`](启动说明.md)。不要在仓库、前端环境变量、浏览器代码或日志中放数据库密码、JWT 密钥、加密密钥、Coze Token 或平台应用密钥。

## 能力边界

Web 客户咨询、持久化会话、人工回复、留资、AI 草稿和 Redis 实时通知是当前实现目标。抖音、快手、视频号和小红书在完成官方权限、协议和真实收发证据前，接口会返回 `PLATFORM_UNVERIFIED`，不能按历史文档配置成“已接入”。
